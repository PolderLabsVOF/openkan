---
description: Inspect and safely mutate OpenKan board tasks, comments, questions, images, and bulk operations.
---

Use `openkan agent context`, then act through `ok task` for lifecycle/ownership
or `openkan api` for board-only operations. Read the task first, preserve active
work unless explicitly changing it, and verify the board after each mutation.

Request: $ARGUMENTS
