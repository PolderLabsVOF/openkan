---
description: Manage durable OpenKan PRDs, goals, plans, milestones, and task rollups.
---

Use `ok prd`, `ok plan`, and `ok task` for durable planning. Use `/api/goals`
for dashboard goal state. Keep plans and evidence synchronized and never create
a second planning store.

Request: $ARGUMENTS
