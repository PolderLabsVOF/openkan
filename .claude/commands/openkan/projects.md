---
description: Manage OpenKan projects, project switching, imports, search, insights, changelog, and settings.
---

Use `/api/projects`, `/api/import`, `/api/search`, `/api/insights/velocity`,
`/api/changelog`, and `/api/config-sections` through `openkan api`. Confirm the
active project and never register Claude worktrees as projects.

Request: $ARGUMENTS
