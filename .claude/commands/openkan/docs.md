---
description: Create, edit, preview, delete, or generate OpenKan docs with the configured agent.
---

Use `/api/docs` via `openkan api`. Read existing content before PUT/DELETE;
keep document paths relative to `docs/`; verify rendering after writes. Use
`POST /api/docs/generate` only with a concrete brief and save/check the output.

Request: $ARGUMENTS
