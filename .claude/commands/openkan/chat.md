---
description: Send, inspect, resume, or abort OpenKan Claude Code chat sessions with full local context.
---

Inspect `/api/chat/sessions` before starting a new session. Send a focused turn
through `/api/chat/send` and watch `/api/chat/events` for live status. Preserve
model, effort, and permission choices; abort only the selected session.

Request: $ARGUMENTS
